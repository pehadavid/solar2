import { FineDurationPipe } from './fine-duration.pipe';

describe('FineDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new FineDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
